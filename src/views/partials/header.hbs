<header>
  <nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-dark px-4">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">LetDine</a>
      {{#if user}}
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse"
        aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav ms-auto mb-2 mb-md-0">
          {{#if user.isAdmin}}
          <li class="nav-item">
            <a href="/admin" class="nav-link text-warning">Admin Dashboard</a>
          </li>
          {{/if}}
          {{#if user.isLeader}}
          <li class="nav-item">
            <a href="/leader" class="nav-link text-warning">Leader Dashboard</a>
          </li>
          {{/if}}
          <li class="nav-item d-inline-flex align-items-center">
            {{#if user}}
            <span id="userId" class="text-white nav-link" data-user-id="{{user.id}}">Greetings, {{ user.email }}</span>{{/if}}
          </li>
          {{!-- <li class="nav-item">
            <a href="/users/{{user.id}}"><button class="btn btn-outline-light my-sm-0 my-2">Profile</button></a>
          </li> --}}
          <li class="nav-item">
            <a href="/logout"><button class="btn btn-outline-danger my-sm-0 my-2">Logout</button></a>
          </li>
        </ul>
      </div>
      {{/if}}
    </div>
  </nav>
</header>